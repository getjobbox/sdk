# JobBox PHP SDK

**Status:** Planned  
**Target package:** `getjobbox/sdk` (Packagist)

This directory will hold the PHP partner SDK. Implementation must follow [`../STANDARD.md`](../STANDARD.md).

Until this package is published, use the HTTP API directly with header `X-JobBox-Api-Key`, or the Node SDK at [`../node`](../node).
